{
  "name": "jsonplace",
  "version": "1.0.0",
  "description": "Render uchun json-server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch users.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
